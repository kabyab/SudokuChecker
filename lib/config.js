/**
 * 
 * Configuration file
 * 
 */

// Container to store all the configurations
var config = {};

// The Configuration values
config.values = {
    'fileName': 'input',
    'extension': '.txt',
    'encoding': 'utf8'
};

// Export the config values
module.exports = config;